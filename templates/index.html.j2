<!doctype html>
<head>
<title>Moa</title>
<!-- <link rel=stylesheet type=text/css href="{{ url_for('static', filename='style.css') }}"> -->
</head>
<body>
<h1>Moa</h1>
{% for message in get_flashed_messages() %}
  <p class=message>{{ message }}</p>
{% endfor %}

{% block body %}
1. {% if g.t_user %}Twitter: {{ g.t_user.screen_name }}{% else %}<a href="{{ url_for('twitter_login') }}">Connect to Twitter</a> {% endif %}

<br>

{% if g.m_user %}2. Mastodon: {{ g.m_user.username }}{% else %}
<form action="{{ url_for('mastodon_login') }}" method="POST">
2. Enter your Mastodon address: <input type=text name=username size=40>
<input type=submit value="GO">
</form>
{% endif %}

<br>

3. Select Options

<br>
4. <a href="{{ url_for('logout') }}">Logout</a>
{% endblock %}

</body>
</html>
